@model IEnumerable<BibleVerse.Models.VerseModel>

@for(int i = 0; i < Model.Count(); i++)
{
    <div class="conatiner-sm">
        <h4>@(Model.ElementAt(i).BookName + " " + Model.ElementAt(i).ChapterId + ":" + Model.ElementAt(i).VerseId)</h4>
        <p>@Model.ElementAt(i).Text</p>
    </div>
}